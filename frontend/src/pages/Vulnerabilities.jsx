import VulnerabilityTable from "../components/VulnerabilityTable";

function Vulnerabilities({ vulnerabilities }) {
  return <VulnerabilityTable vulnerabilities={vulnerabilities} />;
}

export default Vulnerabilities;